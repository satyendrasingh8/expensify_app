import React from "react";
import {Link} from 'react-router-dom';

const HelpPage = () => (
    <div>
     this is from my help page components
     <button><Link to='/'> Go to Dashboard page</Link> </button>

    </div>
    );

    export default HelpPage;